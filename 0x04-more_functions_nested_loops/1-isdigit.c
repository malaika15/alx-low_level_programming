#include "main.h"

/**
* _isdigit - check for a digit from 0 -9
* @c: number to be checked
* Return: 1 if num is a digit else 0
*/


int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1)
;
	else
		return (0);
}
